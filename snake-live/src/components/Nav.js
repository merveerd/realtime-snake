import React from "react";
import { Button } from "./Button";

const Nav = (props) => {
  return (
    <div>
      <Button text="Play with friend" />
      <Button text="Start a game" />
    </div>
  );
};

export { Nav };
